apiVersion: apps/v1
kind: Deployment
metadata: {name: notifications, namespace: microservices}
spec:
  replicas: 1
  selector: {matchLabels: {app: notifications}}
  template:
    metadata: {labels: {app: notifications}}
    spec:
      containers:
      - {name: app, image: hashicorp/http-echo:latest, args: ["-text=Notifications Service", "-listen=:8080"], ports: [{containerPort: 8080}], resources: {limits: {memory: 64Mi, cpu: 50m}}}
---
apiVersion: v1
kind: Service
metadata: {name: notifications, namespace: microservices}
spec: {selector: {app: notifications}, ports: [{port: 80, targetPort: 8080}]}
