# PostgreSQL configuration for RDB
# Development setup with minimal resources

auth:
  postgresPassword: postgres
  database: app

primary:
  persistence:
    enabled: false
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
