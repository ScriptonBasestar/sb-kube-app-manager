description: 빠른 배포 (캐시 활용)
name: quick-deploy
steps:
- condition: '!cache.exists("charts")'
  name: prepare
  type: builtin
- apps:
  - frontend
  - backend
  name: deploy
  parallel: true
  type: builtin
variables:
  use_cache: true
version: '1.0'
