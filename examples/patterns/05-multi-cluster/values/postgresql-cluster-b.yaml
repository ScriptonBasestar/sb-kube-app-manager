auth:
  username: appuser
  password: "cluster-b-pg-pass"
  database: appdb

primary:
  persistence:
    enabled: true
    size: 10Gi
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 500m
      memory: 512Mi

metrics:
  enabled: true
