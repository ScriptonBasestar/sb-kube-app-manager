apiVersion: v1
kind: ConfigMap
metadata:
  name: example-crd
  namespace: action-demo
  labels:
    app: prometheus-operator
data:
  note: |
    This is a placeholder for CRDs.
    In a real scenario, you would install actual CRDs here.

    Example:
    apiVersion: apiextensions.k8s.io/v1
    kind: CustomResourceDefinition
    metadata:
      name: prometheuses.monitoring.coreos.com
    spec:
      ...
