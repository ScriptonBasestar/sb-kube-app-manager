______________________________________________________________________

## type: FAQ audience: End User topics: [faq, common-questions, quick-answers] llm_priority: medium last_updated: 2025-01-04

# 자주 묻는 질문 (FAQ)

## 설정 관련

### Q1. cluster와 kubeconfig_context의 차이는 무엇인가요?

**A**: 두 필드는 완전히 다른 목적을 가집니다:

- **cluster** (선택사항): 사람이 읽을 수 있는 클러스터 식별자

  - 로그와 캐시 디렉토리에만 사용됨
  - kubeconfig의 cluster 이름과 일치할 필요 없음
  - 예: `production-eks`, `dev-k3s`, `staging-gke`

- **kubeconfig_context** (필수): kubectl context 이름

  - `kubectl config get-contexts`의 NAME 컬럼 값
  - `helm --kube-context`와 `kubectl --context`에 전달됨
  - kubeconfig 파일에 실제로 존재해야 함

**예시**:

```yaml
# sources.yaml
cluster: my-production-cluster      # ← 사람용 레이블
kubeconfig: ~/.kube/config
kubeconfig_context: arn:aws:eks:... # ← 실제 kubectl context
```

______________________________________________________________________

### Q2. context를 찾을 수 없다는 오류가 나요

**오류 메시지**:

```
❌ Kubernetes context 'my-context' not found in kubeconfig
```

**해결 방법**:

1. **사용 가능한 contexts 확인**:

   ```bash
   kubectl config get-contexts
   ```

1. **sources.yaml 수정**:

   ```yaml
   kubeconfig_context: <위에서 확인한 NAME 값>
   ```

1. **특정 kubeconfig 파일 사용 시**:

   ```bash
   kubectl config get-contexts --kubeconfig ~/.kube/my-config
   ```

______________________________________________________________________

### Q3. 여러 클러스터를 관리하려면 어떻게 하나요?

**A**: 환경별로 sources.yaml을 분리합니다:

```
config/
├── sources-dev.yaml        # 개발 환경
├── sources-staging.yaml    # 스테이징 환경
└── sources-prod.yaml       # 프로덕션 환경
```

각 파일에 다른 context 설정:

```yaml
# sources-dev.yaml
cluster: dev-k3s
kubeconfig: ~/.kube/config
kubeconfig_context: dev-cluster

# sources-prod.yaml
cluster: production-eks
kubeconfig: ~/.kube/prod-config
kubeconfig_context: production-cluster
```

배포 시 선택 방법:

**방법 1: --profile 단축 옵션 사용 (권장)**

```bash
# sources-dev.yaml 자동 사용
sbkube deploy --profile dev

# sources-prod.yaml 자동 사용
sbkube deploy --profile prod

# sources-staging.yaml 자동 사용
sbkube deploy --profile staging
```

**방법 2: --source 명시적 경로 지정**

```bash
sbkube deploy --source config/sources-prod.yaml
```

> 💡 **Tip**: `--profile` 옵션은 `sources-{profile}.yaml` 파일명 패턴을 따를 때 편리합니다.

______________________________________________________________________

## 클러스터 연결 관련

### Q4. kubeconfig 파일이 여러 개인데 어떻게 선택하나요?

**A**: `kubeconfig` 필드에 절대 경로 또는 `~` 확장 경로 지정:

```yaml
# sources.yaml
kubeconfig: ~/.kube/my-cluster-config  # ← 특정 파일 지정
kubeconfig_context: my-context
```

또는 CLI 옵션으로 오버라이드:

```bash
sbkube deploy --kubeconfig ~/.kube/prod-config --context prod-context
```

______________________________________________________________________

### Q5. KUBECONFIG 환경변수는 사용되나요?

**A**: **아니오**. SBKube는 명시적 설정만 사용합니다:

1. CLI 옵션 (`--kubeconfig`, `--context`)
1. sources.yaml 설정

**이유**: 실수로 잘못된 클러스터에 배포하는 것을 방지하기 위함

______________________________________________________________________

## 배포 관련

### Q6. dry-run으로 배포를 미리 확인할 수 있나요?

**A**: 네, `--dry-run` 옵션을 사용하세요:

```bash
sbkube deploy --app-dir config --namespace test --dry-run
```

템플릿 결과만 확인하려면:

```bash
sbkube template --app-dir config --output-dir /tmp/preview
cat /tmp/preview/myapp/manifests.yaml
```

______________________________________________________________________

### Q7. 특정 단계만 실행할 수 있나요?

**A**: 네, `apply` 명령어에 `--only` 옵션을 사용하세요:

```bash
# template 단계만 실행
sbkube apply --only template

# prepare와 build만 실행
sbkube apply --only prepare,build
```

또는 개별 명령어 사용:

```bash
sbkube prepare
sbkube build
sbkube template
sbkube deploy
```

______________________________________________________________________

## 기타

### Q8. 로그가 너무 많이 나옵니다

**A**: 기본 로그 레벨 변경:

```bash
# 간결한 로그
sbkube deploy --quiet

# 상세 로그 (디버깅 시)
sbkube deploy --verbose
```

______________________________________________________________________

### Q9. 설정 파일 위치를 변경하고 싶어요

**A**: 모든 명령어에서 경로 지정 가능:

```bash
sbkube deploy \
  --base-dir /path/to/project \
  --app-dir custom-config \
  --source custom-config/sources.yaml
```

______________________________________________________________________

### Q10. 배포 상태를 확인하려면?

**A**: `state` 명령어를 사용하세요:

```bash
# 전체 배포 히스토리
sbkube history

# 특정 네임스페이스
sbkube history --namespace production

# 상세 정보
sbkube history --show <deployment-id>
```

______________________________________________________________________

______________________________________________________________________

## v0.8.0 마이그레이션 관련

### Q11. v0.8.0으로 업그레이드해야 하나요?

**A**: 다음 경우 업그레이드 권장:

**✅ 업그레이드가 필요한 경우**:

- 여러 repo의 동일 이름 chart 사용 (예: `grafana/loki`와 `my-company/redis`)
- 동일 chart의 다른 버전 필요 (예: redis 18.0.0과 19.0.0)
- PV/PVC 사용 (수동 프로비저닝)
- 최신 버그 수정 및 보안 패치 필요

**⚠️ 현재 유지 가능한 경우**:

- v0.7.x에서 문제 없이 작동 중
- 단순한 Helm chart 배포만 사용
- 업그레이드 작업 시간이 없음

**참고**: v0.7.x는 계속 사용 가능하지만:

- 버그 수정은 v0.8.0+만 제공
- 신규 기능은 v0.8.0+ 기준
- 보안 패치는 최신 버전만

______________________________________________________________________

### Q12. 마이그레이션이 번거로운데 꼭 해야 하나요?

**A**: 마이그레이션은 **5-10분**이면 완료됩니다:

```bash
# 1. 백업 (선택)
cp -r .sbkube/charts .sbkube/charts.backup

# 2. 업그레이드
uv add sbkube==0.8.0

# 3. 구 charts 제거
rm -rf .sbkube/charts

# 4. 재다운로드
sbkube prepare --force

# 5. 배포
sbkube apply
```

**중요**: Kubernetes 클러스터의 실행 중인 앱은 영향 없음!

**상세 가이드**: [마이그레이션 튜토리얼](../08-tutorials/05-migrating-to-v080.md)

______________________________________________________________________

### Q13. 마이그레이션 중 문제가 생기면?

**A**: 롤백 가능:

**방법 1: 완전 롤백**

```bash
# SBKube 다운그레이드
uv add sbkube==0.7.2

# Charts 재다운로드
rm -rf .sbkube/charts
sbkube prepare

# 배포
sbkube apply
```

**방법 2: 백업 복원** (빠름)

```bash
# 백업 복원
rm -rf .sbkube/charts
mv .sbkube/charts.backup .sbkube/charts

# SBKube 롤백
uv add sbkube==0.7.2

# 배포
sbkube apply
```

______________________________________________________________________

### Q14. Chart 경로가 왜 바뀌었나요?

**A**: 충돌 방지를 위해:

**Before (v0.7.x)**:

```
.sbkube/charts/redis/          # ❌ repo/version 정보 없음
```

- 같은 이름, 다른 repo → 충돌
- 같은 chart, 다른 버전 → 덮어쓰기

**After (v0.8.0)**:

```
.sbkube/charts/grafana/loki-18.0.0/    # ✅ repo + version
.sbkube/charts/grafana/loki-19.0.0/    # ✅ 다른 버전 공존
.sbkube/charts/my-company/redis-1.0.0/  # ✅ 다른 repo 공존
```

**장점**:

- ✅ Chart 충돌 방지
- ✅ 여러 버전 동시 사용
- ✅ 명확한 버전 추적

______________________________________________________________________

### Q15. overrides 경로도 바뀌나요?

**A**: **아니요!** overrides는 그대로 사용:

```
overrides/
└── grafana/          # ✅ 변경 없음
    └── values.yaml
```

**config.yaml도 변경 없음**:

```yaml
apps:
  grafana:
    chart: grafana/grafana
    version: 6.50.0
    overrides:
      - values.yaml    # ✅ 그대로 사용
```

**변경되는 것**: `.sbkube/charts/` 디렉토리 내부 구조만!

______________________________________________________________________

### Q16. PV/PVC 검증이란?

**A**: 배포 전 누락된 PV를 자동으로 감지하는 기능:

```bash
# 검증 실행
sbkube validate

# 예시 출력
✅ Configuration valid
❌ postgresql: PV 'postgresql-hostpath' (8Gi) not found
💡 Create PV before deployment or use dynamic provisioner

# PV 생성 후 재검증
kubectl apply -f pv.yaml
sbkube validate  # ✅ 통과
```

**언제 유용한가?**:

- 수동 PV 프로비저닝 사용 시
- hostPath, local volume 사용 시
- 배포 전 리소스 확인 필요 시

______________________________________________________________________

### Q17. --skip-storage-check는 언제 사용?

**A**: 다음 경우에 사용:

**✅ 사용해야 하는 경우**:

- Dynamic provisioner 사용 (자동 PV 생성)
- 클러스터 접근 불가 (CI/CD 환경)
- 의도적으로 PV를 나중에 생성

**예시**:

```bash
# Dynamic provisioner 사용 시
sbkube validate --skip-storage-check

# AWS EBS, GCE PD 등 자동 생성되는 경우
sbkube apply --skip-storage-check
```

**❌ 사용하지 말아야 하는 경우**:

- hostPath, local volume 사용
- 수동 PV 프로비저닝
- PV 누락으로 배포 실패 위험 시

______________________________________________________________________

### Q18. v0.8.0 성능은 어떤가요?

**A**: 동일하거나 미세 개선:

| 작업 | v0.7.2 | v0.8.0 | 변화 | |------|--------|--------|------| | Chart 다운로드 | 5s | 5s | 동일 | | 빌드 | 3s | 3s | 동일 | | 배포
| 10s | 10s | 동일 | | 검증 | - | 0.5s | **신규** (PV 체크) |

**결론**: 기존 작업은 동일, 검증 기능만 추가됨

______________________________________________________________________

## LLM 친화적 출력 관련

### Q19. LLM 포맷은 무엇인가요?

**A**: AI가 이해하기 쉬운 출력 포맷:

**Human Format (기본)**:

```bash
sbkube status

# 출력 (Rich Console)
✨ Deployment Status ✨
━━━━━━━━━━━━━━━━━━━━━━━━━
┌─────────┬─────────┬─────────┐
│ App     │ Status  │ Version │
├─────────┼─────────┼─────────┤
│ redis   │ running │ 18.0.0  │
└─────────┴─────────┴─────────┘
```

토큰 수: ~500 tokens

**LLM Format**:

```bash
sbkube status --format llm

# 출력 (구조화)
deployment_status:
  redis:
    status: running
    version: 18.0.0
```

토큰 수: ~100 tokens **(80% 절감!)**

______________________________________________________________________

### Q20. 언제 LLM 포맷을 사용하나요?

**A**: 다음 경우에 사용:

**✅ LLM 포맷 사용**:

- AI 에이전트 통합
- 자동화 스크립트
- CI/CD 파이프라인
- 반복적인 모니터링
- 토큰 비용 절감 필요

**✅ Human 포맷 사용**:

- 터미널에서 직접 확인
- 개발 중 디버깅
- 사람이 보는 로그
- 시각적 정보 필요

**예시**:

```bash
# 개발 중 (human)
sbkube status

# CI/CD (llm)
sbkube status --format llm

# 자동화 스크립트 (json)
sbkube status --format json | jq '.summary.failed'
```

**상세 가이드**: [LLM 출력 활용 가이드](../08-tutorials/06-using-llm-output.md)

______________________________________________________________________

### Q21. JSON/YAML 포맷도 지원하나요?

**A**: 네, 4가지 포맷 지원:

| 포맷 | 사용 케이스 | 토큰 수 | |------|------------|---------| | `human` | 터미널, 사람 | 500 | | `llm` | AI, 자동화 | 100 ⭐ | | `json`
| 프로그래밍 | 120 | | `yaml` | 설정 파일 | 110 |

**예시**:

```bash
# JSON으로 출력
sbkube status --format json

# jq로 파싱
sbkube status --format json | jq '.deployment_status.redis.status'

# YAML로 출력
sbkube status --format yaml

# 파일로 저장
sbkube history --format json > history.json
```

______________________________________________________________________

### Q22. 모든 명령어가 LLM 포맷을 지원하나요?

**A**: 대부분 지원하지만 일부 제한:

**✅ 완전 지원** (human, llm, json, yaml):

- `status`
- `history`
- `rollback`
- `validate`

**⚠️ 부분 지원** (프로그레스 제한):

- `apply`
- `prepare`
- `build`
- `deploy`

**이유**: 실시간 프로그레스 바는 human 포맷으로만 표시

**대안**:

```bash
# 로그를 파일로 저장
sbkube deploy --app redis 2>&1 | tee deploy.log

# 또는 quiet 모드
sbkube deploy --app redis --quiet --format llm
```

**향후 계획**: v0.9.0에서 JSONL 스트리밍 지원 예정

______________________________________________________________________

### Q23. 환경 변수로 기본 포맷을 설정할 수 있나요?

**A**: 네, `SBKUBE_OUTPUT_FORMAT` 환경 변수 사용:

```bash
# .bashrc 또는 .zshrc
export SBKUBE_OUTPUT_FORMAT=llm

# 이후 모든 명령어에서 --format 생략 가능
sbkube status  # 자동으로 LLM 포맷 사용
sbkube history # 자동으로 LLM 포맷 사용
```

**일시적으로 변경**:

```bash
# 이번만 JSON 사용
sbkube status --format json

# 환경 변수 무시하고 human 사용
sbkube status --format human
```

______________________________________________________________________

### Q24. LLM 포맷으로 토큰을 얼마나 절감할 수 있나요?

**A**: 약 **80-90% 절감** 가능:

**실제 시나리오**: 10개 앱 배포 모니터링 (1시간에 1회, 24시간)

```
Human 포맷:
- 1회 조회: 500 tokens
- 24시간: 500 × 24 = 12,000 tokens

LLM 포맷:
- 1회 조회: 100 tokens
- 24시간: 100 × 24 = 2,400 tokens

절감: 9,600 tokens (80%)
```

**비용 절감** (GPT-4 기준):

- 입력 토큰: $0.03/1K tokens
- 절감액: ~$0.29/일, ~$8.7/월

**대규모 환경**:

- 100개 앱: ~$87/월 절감
- 1000개 앱: ~$870/월 절감

______________________________________________________________________

## 성능 및 최적화

### Q25. 여러 앱을 동시에 배포할 수 있나요?

**A**: 현재는 순차 배포만 지원:

```bash
# 현재 (v0.8.0) - 순차 배포
sbkube apply --all
# app1 → app2 → app3 순서로 배포
```

**향후 계획** (v0.9.0+):

```bash
# 병렬 배포 (예정)
sbkube apply --all --parallel

# 의존성 고려한 병렬 배포 (예정)
sbkube apply --all --parallel --respect-deps
```

**현재 대안**:

- 의존성 없는 앱은 별도 터미널에서 동시 실행
- CI/CD에서 job 분할

______________________________________________________________________

### Q26. Chart 캐시를 어떻게 관리하나요?

**A**: `.sbkube/charts/`는 자동으로 관리됨:

**캐시 확인**:

```bash
# Chart 목록 확인
ls -R .sbkube/charts/

# 크기 확인
du -sh .sbkube/charts/
```

**캐시 정리**:

```bash
# 전체 삭제 (재다운로드 필요)
rm -rf .sbkube/charts

# 재다운로드
sbkube prepare --force
```

**자동 정리** (v0.8.0):

- 사용하지 않는 버전은 자동으로 유지
- 명시적 삭제 없이는 보존

**주의**: `.sbkube/`는 Git에 절대 커밋하지 마세요!

______________________________________________________________________

## 참고 자료

- [트러블슈팅 가이드](README.md)
- [설정 스키마](../03-configuration/config-schema.md)
- [명령어 참조](../02-features/commands.md)
- [v0.8.0 마이그레이션 가이드](../08-tutorials/05-migrating-to-v080.md)
- [Chart 충돌 문제 해결](./chart-collision-issues.md)
- [LLM 출력 활용 가이드](../08-tutorials/06-using-llm-output.md)
