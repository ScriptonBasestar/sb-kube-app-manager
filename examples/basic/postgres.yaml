# PostgreSQL values override

# Authentication
auth:
  username: demo
  password: "demo-password"  # Change this in production!
  database: demoapp

# Primary configuration
primary:
  persistence:
    enabled: false  # Disable persistence for demo
