# SBKube v0.3.0 Configuration
# Relational databases: PostgreSQL and MariaDB

namespace: data

apps:
  postgresql:
    type: helm
    chart: bitnami/postgresql
    values:
      - postgresql.yaml
    # overrides:
    #   - templates/pg-host-vol.yaml
    #   - templates/primary/statefulset.yaml
    # removes:
    #   - files/aaa.conf

  mariadb:
    type: helm
    chart: bitnami/mariadb
    values:
      - mariadb.yaml
    # overrides:
    #   - templates/mariadb-vol.yaml
    #   - templates/primary/statefulset.yaml
