# Green environment active, blue idle
namespace: bluegreen-demo

global_labels:
  deployment-strategy: blue-green
  managed-by: sbkube

apps:
  backend-blue:
    type: helm
    chart: bitnami/nginx
    version: "15.0.0"
    values:
      - values/backend-blue-idle.yaml
    labels:
      app: backend
      version: blue
      environment: blue
      status: idle
    enabled: true

  backend-green:
    type: helm
    chart: bitnami/nginx
    version: "15.0.0"
    values:
      - values/backend-green.yaml
    labels:
      app: backend
      version: green
      environment: green
      status: active
    enabled: true
