apiVersion: v1
kind: ConfigMap
metadata: {name: recovery-plan, namespace: dr-demo}
data:
  plan.yaml: |
    # Disaster Recovery Plan
    # 1. Verify backup: velero backup get
    # 2. Restore: velero restore create --from-backup <name>
    # 3. Validate: kubectl get all -A
