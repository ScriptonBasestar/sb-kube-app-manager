auth:
  enabled: true
  password: "cluster-a-redis-pass"

replica:
  replicaCount: 2

master:
  persistence:
    enabled: true
    size: 8Gi
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi

metrics:
  enabled: true
