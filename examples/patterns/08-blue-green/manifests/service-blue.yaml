apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: bluegreen-demo
  labels:
    app: backend
    active-environment: blue  # Updated during switch
spec:
  selector:
    app: backend
    environment: blue
    status: active
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: LoadBalancer
