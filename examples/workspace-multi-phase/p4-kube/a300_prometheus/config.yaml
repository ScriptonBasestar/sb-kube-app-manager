# Phase 4 - Prometheus Monitoring
namespace: monitoring

apps:
  prometheus:
    type: helm
    enabled: true
    chart: prometheus-community/prometheus
    version: "25.0.0"
    values:
      - values.yaml
