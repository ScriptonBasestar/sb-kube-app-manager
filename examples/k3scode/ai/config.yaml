# SBKube Configuration
# AI/ML tools: Toolhive operator

namespace: toolhive

apps:
  toolhive-operator:
    type: git
    repo: stacklok-toolhive
    path: deploy/charts/operator
    # Note: SBKube git type supports single path
    # For multi-path, use separate apps or manual copy

  # If you need local charts from git repo:
  # toolhive-operator-chart:
  #   type: helm
  #   chart: ./repos/stacklok-toolhive/deploy/charts/operator
  #   values:
  #     - toolhive-operator.yaml
  #
  # toolhive-operator-crds:
  #   type: helm
  #   chart: ./repos/stacklok-toolhive/deploy/charts/operator-crds
  #   values:
  #     - toolhive-operator-crds.yaml
