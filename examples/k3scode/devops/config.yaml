# SBKube v0.3.0 Configuration
# DevOps tools: Custom proxy

namespace: devops

apps:
  proxynd-custom:
    type: helm
    chart: ./proxynd-custom  # Local chart (no copy needed in v0.3.0)
    values:
      - proxynd-custom.yaml
    # Note: v0.3.0 automatically uses local charts
    # No need for separate copy-app step
